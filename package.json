{
  "name": "un-stuff-my-feed",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "extension",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w client\" \"npm run dev -w server\"",
    "build": "npm run build -w shared && npm run build -w client && npm run build -w server && npm run build -w extension",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
